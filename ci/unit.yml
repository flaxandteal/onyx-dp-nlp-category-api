---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3.9

inputs:
  - name: dp-nlp-category-api

caches:
  - path: build/

run:
  path: dp-nlp-category-api/ci/scripts/unit.sh
